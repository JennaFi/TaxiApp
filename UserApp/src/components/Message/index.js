import React from 'react'
import { Text, View } from 'react-native'
import styles from './styles'

const Message = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>Travel only if necessary</Text>
            <Text style={styles.text}>Travel only if necessary Travel only if necessary Travel only if necessary Travel  Travel only if necessaryonly if necessary Travel only if necessary Travel only if necessary</Text>
            <Text style={styles.learnMore}>Learn more</Text>
        </View>
    )
}

export default Message
